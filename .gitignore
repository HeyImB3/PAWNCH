# Dependencies
node_modules/
server/node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Build / temp
dist/
build/
.cache/
*.tmp

# Asset-tool QA scratch (regenerated by tools/slice_mk3.py)
tools/_mk3_montage.png
tools/_mk3_probe.png

# Local env
.env
.env.local

# Python sprite-tool venv
tools/.venv/

# Raw AI generations (large; regenerable). Only the sliced frames are committed.
assets/sprites/_src/
# Throwaway QA artifacts from the sprite tools (montages, audit/contact sheets, lineups…)
tools/_*.png

# Claude local-only settings (keep launch.json so others can run the preview)
.claude/settings.local.json

# Superpowers brainstorm mockups (transient; specs/plans live in docs/)
.superpowers/
